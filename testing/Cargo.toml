[package]
name = "bridge-testing"
version = "0.1.0"
edition = "2024"

[dependencies]
bridge-core = { path = "../core" }
bridge = { path = "../app" }
tokio = { version = "1", features = ["sync", "rt", "time"] }
