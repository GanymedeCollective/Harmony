[package]
name = "bridge-core"
version = "0.1.0"

edition.workspace = true
license.workspace = true

[dependencies]
http.workspace = true
tokio = { workspace = true, features = ["sync"] }

[lints]
workspace = true
